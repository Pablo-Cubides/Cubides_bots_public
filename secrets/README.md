# Secrets

This template repository does not include real encrypted secrets.

Create your own local SOPS/AGE setup or use the provided `*.example` files and setup scripts as references. Never commit real `.env`, runtime env files, OAuth refresh tokens, Slack tokens, Gmail app passwords, API keys, or AGE private keys.

