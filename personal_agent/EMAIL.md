# Personal Agent Email Template

Configure a dedicated email account for the agent using secrets setup scripts. Do not commit real addresses or app passwords.

Use `user@example.com` as a placeholder in public documentation.

