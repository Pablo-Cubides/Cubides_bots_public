---
paths:
  - "business_agent/**/*.py"
  - "personal_agent/**/*.py"
---

# Python rules

- Keep changes minimal and focused on requested behavior.
- Preserve existing API contracts and endpoint names.
- Prefer explicit error handling and clear logs.
- Avoid introducing new dependencies unless needed.

